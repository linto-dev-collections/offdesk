# Workers ランタイム（workerd）と Vite の解決を揃えるため、依存の巻き上げをしない。
# 巻き上げると、宣言していないパッケージがローカルでは解決できて本番では落ちる。
node-linker=isolated

# peerDependencies の不一致で install ごと止めない。React 19 に追随していない
# 周辺パッケージが出るたびに install が通らなくなると、版を上げる作業ができない。
strict-peer-dependencies=false
