# 依存とビルド生成物
node_modules
dist
*.tsbuildinfo

# ツールのキャッシュ
.turbo
.wrangler
.tanstack

# Alchemy の状態ファイル。シークレットが ALCHEMY_PASSWORD で暗号化されて入る。
.alchemy

# 秘密。**.env.example だけを commit する。**
.env
.env.local
.env*.local

# offdesk 自身の plans/ は commit する（対象リポジトリ側の規約とは別物）。
# ここに /plans/ を書かないのは意図であって漏れではない。

# エディタ / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.log

# plans
plans

# docs
docs
